export default function AppHeader() {

    const title = "Task Manager";

    // possiamo anche fare il return della variabile header

    return (
        <header>
            <h1>{title}</h1>
        </header>
    )
}